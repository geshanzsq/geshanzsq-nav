# 项目查询框架与接口开发实战指南

## 创新点、解决前、解决后

### 创新点

本项目在 MyBatis-Plus 之上，围绕“后台列表查询、分页查询、排序查询、条件自动组装”做了统一封装，核心组合为：

- `@Query`
- `QueryWrapperPlus`
- `LambdaQueryWrapperPlus`
- `BaseMapperPlus`
- `BaseService` / `BaseServiceImpl`

它的创新点不在于单纯“包一层”，而在于把查询条件的定义从 Service/Mapper 中前移到 DTO 字段声明上，实现了“查询参数定义”和“查询条件构建规则”的收敛。

### 解决前

- 每个分页/列表接口都要手写 `QueryWrapper` 或 `LambdaQueryWrapper`。
- `eq`、`like`、`in`、时间范围、排序等逻辑大量重复。
- DTO 字段、实体字段、数据库列之间的映射规则分散在多处代码里。
- 新增一个筛选条件，往往要同时改 DTO、Service、Mapper 查询逻辑。
- 不同开发人员写法不统一，新人很难快速判断项目推荐姿势。

### 解决后

- 简单单表查询可直接用 `DTO + @Query` 自动生成条件。
- 分页、列表、排序、单条、计数查询都有统一入口。
- DTO 与实体字段不一致时，可用 `fieldName` 明确映射。
- 复杂场景可在自动条件基础上继续补 `QueryWrapperPlus` / `LambdaQueryWrapper` 条件。
- 新人只要掌握一套规则，就能快速按项目风格开发查询接口。

---

## 1. 文档目标

本文档结合现有项目实现，整理出一份**以当前仓库实际代码为准**的查询开发指南，帮助新人理解：

- 当前项目查询封装的整体设计。
- `@Query`、`BaseService`、`BaseMapperPlus`、`QueryWrapperPlus` 的职责。
- 标准分页/列表接口应该怎么写。
- 什么时候用自动查询，什么时候手写 Wrapper。
- 当前项目已支持什么，哪些是可继续扩展的方向。

---

## 2. 整体设计思路

当前项目的典型查询链路是：

`Controller 入参 DTO -> Service -> BaseService/BaseMapperPlus -> QueryWrapperPlus -> MyBatis-Plus`

其中：

- `DTO`：承接前端查询参数。
- `@Query`：定义字段如何参与查询。
- `QueryWrapperPlus`：读取 DTO 字段并自动拼接 where/order by。
- `BaseMapperPlus`：提供统一的分页、列表、排序、计数、单条查询能力。
- `BaseService`：把这些通用查询能力继续向 Service 层透出。

这套封装最重要的目标有三个：

- 查询条件声明式化。
- 通用查询模板化。
- 复杂查询扩展化。

---

## 3. 核心组件说明

### 3.1 `@Query`

位置：

- `geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/mybatis/plugin/annotation/Query.java`

定义：

```java
public @interface Query {
    QueryWay value() default QueryWay.EQ;
    String fieldName() default "";
    boolean ignore() default false;
    boolean empty() default false;
}
```

字段说明：

- `value`：查询方式，默认 `EQ`。
- `fieldName`：当 DTO 字段名和实体属性名不一致时，指定映射到哪个实体属性。
- `ignore`：忽略该字段，不参与自动查询。
- `empty`：值为空时是否仍参与查询，默认 `false`。

典型示例：

```java
@Query(QueryWay.LIKE)
private String username;

@Query(value = QueryWay.GE, fieldName = "gmtCreate")
private Date beginGmtCreate;

@Query(ignore = true)
private String siteNameDescription;
```

结论：

- 没加 `@Query`，默认按同名字段做 `EQ`。
- 不参与自动查询的字段，必须显式 `ignore = true`。

### 3.2 `QueryWay`

位置：

- `geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/mybatis/plugin/enums/QueryWay.java`

当前项目已支持：

- `EQ`
- `NE`
- `GT`
- `GE`
- `LT`
- `LE`
- `LIKE`
- `NOT_LIKE`
- `LIKE_LEFT`
- `LIKE_RIGHT`
- `IN`
- `NOT_IN`

对应语义：

| QueryWay | 说明 | 典型 SQL |
| --- | --- | --- |
| `EQ` | 等于 | `column = ?` |
| `NE` | 不等于 | `column <> ?` |
| `GT` | 大于 | `column > ?` |
| `GE` | 大于等于 | `column >= ?` |
| `LT` | 小于 | `column < ?` |
| `LE` | 小于等于 | `column <= ?` |
| `LIKE` | 两侧模糊 | `column like '%xx%'` |
| `LIKE_LEFT` | 左模糊 | `column like '%xx'` |
| `LIKE_RIGHT` | 右模糊 | `column like 'xx%'` |
| `IN` | 包含 | `column in (...)` |
| `NOT_IN` | 不包含 | `column not in (...)` |

### 3.3 `QueryWrapperPlus`

位置：

- `geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/mybatis/plugin/query/QueryWrapperPlus.java`

职责：

- 基于 DTO 和实体类元数据，自动构建 MyBatis-Plus `QueryWrapper`。

它实际做的事情：

1. 获取实体类属性和数据库列的映射关系。
2. 反射遍历 DTO 字段。
3. 读取每个字段上的 `@Query`。
4. 校验查询字段能否映射到实体属性。
5. 根据 `QueryWay` 自动拼装查询条件。
6. 自动识别 `orderColumn`、`orderType`，拼装排序。

需要新人重点记住的规则：

- DTO 字段没加 `@Query`，默认按 `EQ` 查询。
- 字段值为空时，默认不拼接查询条件。
- `fieldName` 填的是**实体属性名**，不是数据库列名。
- `orderColumn` 传的也是**实体属性名**，不是数据库列名。
- DTO 中非查询字段若未 `ignore = true`，框架仍会尝试映射，映射不到就抛异常。

### 3.4 `LambdaQueryWrapperPlus`

位置：

- `geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/mybatis/plugin/query/LambdaQueryWrapperPlus.java`

职责：

- 给手写 Lambda 查询补充 `xxxIf` 风格方法，减少判空样板代码。

当前项目已提供：

- `likeIf`
- `eqIf`
- `inIf`
- `notInIf`
- `ltIf`
- `leIf`
- `gtIf`
- `geIf`

适用场景：

- 复杂查询不适合完全放在 DTO 注解里。
- 想保留 Lambda 写法，同时减少 `if (x != null)`。

### 3.5 `BaseMapperPlus`

位置：

- `geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/web/mapper/BaseMapperPlus.java`

职责：

- 在 MyBatis-Plus `BaseMapper` 基础上统一封装通用查询。

常用方法：

- `selectPage(D d, SFunction<T, ?>... selectColumns)`
- `selectPageAsc(D d, SFunction<T, ?>... ascColumns)`
- `selectPageDesc(D d, SFunction<T, ?>... descColumns)`
- `selectList(D d, SFunction<T, ?>... selectColumns)`
- `selectListAsc(D d, SFunction<T, ?>... ascColumns)`
- `selectListDesc(D d, SFunction<T, ?>... descColumns)`
- `selectOne(D d)`
- `selectCount(D d)`
- `buildQueryWrapper(D d, ...)`

作用可以概括为一句话：

- 只要 Mapper 继承 `BaseMapperPlus<T>`，就天然具备一套标准查询能力。

### 3.6 `BaseService` / `BaseServiceImpl`

位置：

- `geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/web/service/BaseService.java`
- `geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/web/service/impl/BaseServiceImpl.java`

职责：

- 把 `BaseMapperPlus` 的通用查询能力继续下沉到 Service 层。

常用方法：

- `page(d)`
- `pageAsc(d, ...)`
- `pageDesc(d, ...)`
- `list(d)`
- `listAsc(d, ...)`
- `listDesc(d, ...)`
- `getOne(d)`
- `count(d)`
- `buildQueryWrapper(d, ...)`

适合场景：

- Service 里直接用统一查询能力，不再重复写分页/排序模板代码。

---

## 4. 查询执行链路

以一个典型分页列表接口为例，执行链路如下：

1. Controller 接收查询 DTO。
2. Service 调用 `mapper.selectPage(dto)` 或 `page(dto)`。
3. `BaseMapperPlus` / `BaseService` 内部调用 `buildQueryWrapper(dto)`。
4. `QueryWrapperPlus` 反射读取 DTO 字段与 `@Query` 注解。
5. 自动生成 where 条件与排序条件。
6. MyBatis-Plus 执行 SQL。
7. Service 再把 PO 转成 VO 返回。

文字版链路：

```text
Controller
  -> DTO
  -> Service
  -> BaseService / BaseMapperPlus
  -> QueryWrapperPlus.buildQueryWrapper(...)
  -> MyBatis-Plus selectList/selectPage
  -> VO / PageVO
```

---

## 5. 自动查询规则

### 5.1 默认规则：没写 `@Query` 也会参与查询

例如：

```java
private Integer status;
```

只要：

- 字段有值。
- 字段名能映射到实体属性。

那么就会默认生成：

```sql
and status = ?
```

### 5.2 空值默认跳过

如果字段值为空，且没有设置 `empty = true`，则不会拼接条件。

常见表现：

- `name = null`：不会拼 `LIKE`
- `ids = []`：不会拼 `IN`
- `beginTime = null`：不会拼 `>=`

### 5.3 `ignore = true` 会完全忽略

示例：

```java
@Query(ignore = true)
private String keyword;
```

适合：

- 业务中间参数。
- 仅用于 Service 手工补条件的字段。
- 当前实体不存在的辅助字段。

### 5.4 `fieldName` 用于字段映射

示例：

```java
@Query(value = QueryWay.IN, fieldName = "id")
private List<Long> ids;
```

含义：

- 请求参数字段叫 `ids`
- 实际查询实体属性 `id`

### 5.5 字段必须能映射到实体属性

如果：

- 字段没有 `ignore = true`
- 又没有 `fieldName`
- 实体类中也找不到同名属性

那么 `QueryWrapperPlus` 会直接抛错。

这也是为什么：

- DTO 不是普通“随便放参数”的对象。
- DTO 中每个字段都要明确知道自己是否参与自动查询。

---

## 6. 排序与分页规则

### 6.1 `PageDTO`

位置：

- `geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/mybatis/page/dto/PageDTO.java`

内置字段：

- `pageNum`
- `pageSize`
- `orderColumn`
- `orderType`

这些字段都已标记 `@Query(ignore = true)`，不会误拼进 where。

### 6.2 分页

分页 DTO 一般继承 `PageDTO`。

例如：

```java
public class SysUserPageDTO extends PageDTO implements Serializable {
}
```

常见调用：

```java
sysUserMapper.selectPage(pageDTO);
page(pageDTO);
pageDesc(pageDTO, SysUser::getGmtCreate);
```

统一返回：

```java
PageVO<T>
```

包含：

- `list`
- `total`

### 6.3 动态排序

可以通过 DTO 传：

```java
pageDTO.setOrderColumn("gmtCreate,id");
pageDTO.setOrderType("desc,desc");
```

规则：

- 多字段用英文逗号分隔。
- `orderColumn` 必须传实体属性名。
- `orderType` 可缺省，缺省时默认升序。
- 若传入非法排序字段，会抛异常。

### 6.4 固定排序

也可以直接在代码里指定：

```java
pageDesc(pageDTO, SysUser::getGmtCreate, SysUser::getId);
listAsc(dto, SysMenu::getSort, SysMenu::getParentId);
```

建议：

- 后台固定排序优先写死在 Service。
- 确实要开放前端排序时，再使用 `orderColumn/orderType`。

---

## 7. 当前项目里的标准写法

### 7.1 分页查询 DTO

示例：`SysUserPageDTO`

```java
public class SysUserPageDTO extends PageDTO implements Serializable {

    @Query(QueryWay.LIKE)
    private String username;

    @Query(QueryWay.LIKE)
    private String nickName;

    private Integer status;

    @Query(value = QueryWay.GE, fieldName = "gmtCreate")
    private Date beginGmtCreate;

    @Query(value = QueryWay.LE, fieldName = "gmtCreate")
    private Date endGmtCreate;

    @Query(value = QueryWay.IN, fieldName = "id")
    private List<Long> ids;

    @Query(value = QueryWay.NOT_IN, fieldName = "id")
    private List<Long> notIds;
}
```

这体现了当前项目最常见的几种查询写法：

- 默认等值查询
- `LIKE`
- 开始时间/结束时间映射同一实体字段
- `IN` / `NOT_IN`

### 7.2 Mapper 写法

```java
public interface SysUserMapper extends BaseMapperPlus<SysUser> {
}
```

### 7.3 ServiceImpl 写法

```java
@Service
public class SysUserServiceImpl extends BaseServiceImpl<SysUserMapper, SysUser>
        implements SysUserService {
}
```

### 7.4 最简单的分页接口

```java
public PageVO<SysUserVO> pageList(SysUserPageDTO pageDTO) {
    pageDTO.setOrderColumn("gmtCreate,id");
    return SysUserConverter.INSTANCE.convert(sysUserMapper.selectPage(pageDTO));
}
```

### 7.5 最简单的列表接口

```java
public List<SysMenuVO> getMenuList(SysMenuListDTO dto) {
    dto.setOrderColumn("sort,parentId");
    return SysMenuConverter.INSTANCE.convertList(sysMenuMapper.selectList(dto));
}
```

这类接口的核心原则是：

- DTO 负责定义条件。
- Service 负责少量业务补充和固定排序。
- Mapper 通用查询由框架封装承担。

---

## 8. 自动查询不够用时怎么扩展

### 8.1 推荐方式：自动条件 + 手工补条件

项目里的典型例子是 `NavSiteServiceImpl.pageList`。

```java
public PageVO<NavSiteVO> pageList(NavSitePageDTO pageDTO) {
    QueryWrapperPlus<NavSite> wrapper = buildQueryWrapper(pageDTO);
    if (StrUtils.isNotBlank(pageDTO.getSiteNameDescription())) {
        wrapper.lambda().and(w -> {
            w.like(NavSite::getSiteName, pageDTO.getSiteNameDescription())
                .or()
                .like(NavSite::getSiteDescription, pageDTO.getSiteNameDescription());
        });
    }
    return NavSiteConverter.INSTANCE.convert(navSiteMapper.selectPage(pageDTO, wrapper));
}
```

这个写法是当前项目里最推荐的复杂查询姿势：

- 通用字段仍走 `@Query`
- 复合字段手工补充
- 风格统一，扩展也灵活

这里 `siteNameDescription` 之所以写成：

```java
@Query(ignore = true)
private String siteNameDescription;
```

就是为了避免框架先默认按同名字段拼错条件。

### 8.2 手写 `LambdaQueryWrapper`

如果查询逻辑本身就不适合 DTO 自动查询，可以直接手写：

```java
LambdaQueryWrapper<SysUser> wrapper = new LambdaQueryWrapper<>();
wrapper.eq(SysUser::getUsername, username);
SysUser sysUser = sysUserMapper.selectOne(wrapper);
```

典型场景：

- 登录校验
- 唯一性校验
- 树结构查询
- 非列表类的小型业务查询

### 8.3 用 `LambdaQueryWrapperPlus` 减少判空代码

```java
LambdaQueryWrapperPlus<SysUser> wrapper = new LambdaQueryWrapperPlus<>();
wrapper.likeIf(SysUser::getUsername, username)
       .eqIf(SysUser::getStatus, status)
       .inIf(SysUser::getId, ids);
```

适合：

- 想自己控制查询逻辑。
- 但不想写很多判空 `if`。

---

## 9. 一条新查询接口应该怎么开发

### 场景一：普通分页列表

1. 新建 `XXXPageDTO`，继承 `PageDTO`。
2. 给查询字段补 `@Query`。
3. `Mapper` 继承 `BaseMapperPlus<PO>`。
4. `ServiceImpl` 继承 `BaseServiceImpl<Mapper, PO>`。
5. 直接调用 `mapper.selectPage(dto)` 或 `page(dto)`。
6. 需要固定排序时，在 Service 设置 `orderColumn` 或调用 `pageAsc/pageDesc`。

### 场景二：普通列表

1. 新建 `XXXListDTO`。
2. 给查询字段补 `@Query`。
3. 直接 `mapper.selectList(dto)` 或 `list(dto)`。

### 场景三：自动查询不够用

1. 先 `buildQueryWrapper(dto)` 生成基础条件。
2. 再手工补充 `and/or`、复合 like、额外过滤条件。
3. 最后调用 `selectPage(pageDTO, wrapper)` 或 `selectList(wrapper)`。

---

## 10. 推荐代码风格

### 推荐

- 简单单表查询优先使用 `DTO + @Query`。
- DTO 字段名尽量与实体属性保持一致。
- 复合查询字段明确标记 `@Query(ignore = true)`。
- 固定排序尽量放在 Service 层统一设置。
- 一个 DTO 只表达一个明确查询场景。

### 不推荐

- 每个列表页都从头手写 `LambdaQueryWrapper`。
- 在 Controller 中拼接查询条件。
- `fieldName` 或 `orderColumn` 传数据库列名。
- 把中间业务字段直接塞进 DTO 却不加 `ignore = true`。

---

## 11. 常见坑位

### 11.1 DTO 字段与实体属性不一致，却没写 `fieldName`

后果：

- `QueryWrapperPlus` 校验失败，直接抛异常。

### 11.2 排序字段传数据库列名

错误示例：

- `gmt_create`

正确示例：

- `gmtCreate`

### 11.3 复合搜索字段没加 `ignore = true`

后果：

- 自动查询先拼一套默认条件
- 业务代码又手工补一套
- 最终 where 条件不符合预期

### 11.4 所有场景都强行走自动查询

自动查询适合单表常规筛选，不适合：

- 多表联查
- 聚合统计
- 分组查询
- 复杂 OR 嵌套
- 依赖复杂业务计算的动态条件

这类场景应回到：

- 自定义 Mapper SQL
- 或手工 Wrapper 构造

---

## 12. 可直接复用的模板

### 分页 DTO 模板

```java
@Data
public class XxxPageDTO extends PageDTO implements Serializable {

    private static final long serialVersionUID = 1L;

    @Query(QueryWay.LIKE)
    private String name;

    private Integer status;

    @Query(value = QueryWay.GE, fieldName = "gmtCreate")
    private Date beginGmtCreate;

    @Query(value = QueryWay.LE, fieldName = "gmtCreate")
    private Date endGmtCreate;

    @Query(ignore = true)
    private String keyword;
}
```

### Mapper 模板

```java
public interface XxxMapper extends BaseMapperPlus<Xxx> {
}
```

### ServiceImpl 模板

```java
@Service
public class XxxServiceImpl extends BaseServiceImpl<XxxMapper, Xxx> implements XxxService {

    @Autowired
    private XxxMapper xxxMapper;

    public PageVO<XxxVO> pageList(XxxPageDTO pageDTO) {
        QueryWrapperPlus<Xxx> wrapper = buildQueryWrapper(pageDTO);
        if (StrUtils.isNotBlank(pageDTO.getKeyword())) {
            wrapper.lambda().and(w -> {
                w.like(Xxx::getName, pageDTO.getKeyword())
                 .or()
                 .like(Xxx::getDescription, pageDTO.getKeyword());
            });
        }
        return XxxConverter.INSTANCE.convert(xxxMapper.selectPage(pageDTO, wrapper));
    }
}
```

---

## 13. 一句话总结

当前项目查询接口开发遵循这条原则：

**单表标准查询优先使用 `DTO + @Query + BaseMapperPlus/BaseService`；复杂条件优先基于 `buildQueryWrapper` 继续补充；只有在自动化明显不适合时，才直接手写 Wrapper 或自定义 SQL。**

按这个原则写，代码会与现有项目风格保持一致，维护成本也最低。
