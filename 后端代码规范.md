# 后端代码规范

## 1. 适用范围

本文基于当前仓库后端代码、Maven 配置和典型实现归纳，适用于：

- `geshanzsq-nav-admin`
- `geshanzsq-nav-common`

说明：

- 当前项目未发现 `checkstyle`、`spotless`、`pmd`、`sonar` 一类强制静态检查插件。
- 因此后端规范以“现有项目约定”为主，属于团队风格规范，不完全等同于编译期硬限制。

## 2. 技术基线

- JDK：`1.8`
- Maven 编码：`UTF-8`
- Spring Boot：`2.3.12.RELEASE`
- ORM：`MyBatis Plus 3.5.2`
- 对象转换：`MapStruct 1.5.0.RC1`
- 简化实体：`Lombok 1.18.22`

对应依据：

- 根目录 [pom.xml](/D:/Project/myself/geshanzsq-nav/pom.xml)

## 3. 模块与分层规则

后端整体采用清晰分层，新增功能应保持同样结构：

- `controller`：接口入口层，只做参数接收、权限注解、调用服务、统一响应返回。
- `service` / `service.impl`：业务逻辑层，负责业务校验、事务控制、组合查询。
- `mapper`：数据库访问层。
- `resources/mapper/**/*.xml`：复杂 SQL 放 XML，不把复杂 SQL 堆到业务层。
- `po`：数据库持久化对象。
- `dto`：请求参数对象。
- `vo`：响应对象。
- `mapstruct`：DTO、PO、VO 之间的对象转换。
- `constant` / `enums` / `util`：常量、枚举、工具类按职责独立放置。

推荐链路：

- `Controller -> Service -> Mapper`
- `DTO -> Converter -> PO`
- `PO -> Converter -> VO`

不建议：

- `Controller` 直接操作 `Mapper`
- `Controller` 内拼装复杂业务对象
- 直接把 `PO` 暴露给前端

## 4. 命名规范

项目当前命名具有较强一致性，应继续保持：

- 控制器：`XxxController`
- 服务接口：`XxxService`
- 服务实现：`XxxServiceImpl`
- Mapper：`XxxMapper`
- 持久化对象：`Xxx`
- 请求对象：`XxxAddDTO`、`XxxUpdateDTO`、`XxxPageDTO`、`XxxListDTO`
- 响应对象：`XxxVO`
- 转换器：`XxxConverter`

常见语义后缀：

- 新增：`AddDTO`
- 修改：`UpdateDTO`
- 分页：`PageDTO`
- 列表筛选：`ListDTO`
- 详情/展示：`VO`

包路径按业务域拆分，例如：

- `com.geshanzsq.admin.system.menu`
- `com.geshanzsq.admin.nav.simple.site`

新增功能时应先归属业务域，再在域下补齐分层目录。

## 5. Controller 规范

### 5.1 统一返回结构

所有接口统一使用 `ResponseResult<T>` 返回，不直接返回裸对象。

示例来源：

- [ResponseResult.java](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-common/geshanzsq-nav-common-core/src/main/java/com/geshanzsq/common/core/web/response/ResponseResult.java)
- [SysMenuController.java](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-admin/geshanzsq-nav-admin-system/src/main/java/com/geshanzsq/admin/system/menu/controller/SysMenuController.java)

推荐写法：

- 查询成功：`ResponseResult.success(data)`
- 无返回体操作：`ResponseResult.success()`
- 失败走统一异常体系，避免手写大量分支返回

### 5.2 参数接收

- 查询类接口优先直接接收 `DTO`
- JSON Body 入参统一使用 `@RequestBody`
- 需要校验时使用 `@Valid`
- 路径参数使用 `@PathVariable`

现有惯例：

- `public ResponseResult add(@Valid @RequestBody XxxAddDTO addDTO)`

### 5.3 Controller 职责边界

Controller 只保留以下内容：

- 路由注解
- Swagger 注解
- 权限注解
- 操作日志注解
- 参数绑定与简单转发

不要在 Controller 中写：

- 复杂条件判断
- 多表事务逻辑
- 手工对象转换
- 大段 SQL 相关逻辑

## 6. DTO / VO / PO 规范

### 6.1 DTO

DTO 负责承载请求参数，要求：

- 实现 `Serializable`
- 使用 `lombok.Data`
- 字段校验使用 `javax.validation` 注解
- 补充 Swagger 注解说明字段含义

典型示例：

- [SysMenuAddDTO.java](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-admin/geshanzsq-nav-admin-system/src/main/java/com/geshanzsq/admin/system/menu/dto/SysMenuAddDTO.java)

### 6.2 VO

VO 只用于接口输出：

- 不直接承载数据库写入语义
- 可根据前端展示需要组织字段
- 分页统一使用 `PageVO<T>`

### 6.3 PO

PO 与表结构对应，要求：

- 字段名采用 Java 驼峰
- 与数据库字段不一致时使用 MyBatis Plus 注解映射
- 持久化对象不混入前端展示特有字段

典型示例：

- [SysMenu.java](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-admin/geshanzsq-nav-admin-system/src/main/java/com/geshanzsq/admin/system/menu/po/SysMenu.java)

## 7. 对象转换规范

项目统一使用 MapStruct 进行对象转换。

要求：

- 每个业务域单独维护 `XxxConverter`
- `INSTANCE = Mappers.getMapper(XxxConverter.class)` 作为统一入口
- 转换逻辑尽量放在 Converter，不在 Controller/Service 手写 Bean copy

典型示例：

- [SysMenuConverter.java](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-admin/geshanzsq-nav-admin-system/src/main/java/com/geshanzsq/admin/system/menu/mapstruct/SysMenuConverter.java)

推荐：

- `DTO -> PO`
- `PO -> VO`
- `PageVO<PO> -> PageVO<VO>`

## 8. Service 规范

### 8.1 继承基类

项目大量服务实现继承：

- `BaseServiceImpl<M extends BaseMapperPlus<T>, T>`

参考：

- [BaseServiceImpl.java](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/web/service/impl/BaseServiceImpl.java)

这意味着新增服务优先复用：

- MyBatis Plus 通用 CRUD
- 项目封装的分页能力

### 8.2 业务逻辑要求

Service 层负责：

- 参数合法性校验
- 业务存在性校验
- 事务控制
- 组合查询与数据组装
- 权限相关的业务决策

典型模式：

- 删除前先校验是否存在子数据
- 关联写操作加 `@Transactional(rollbackFor = Exception.class)`
- 用 `ServiceException` / `ParamException` 抛业务异常

典型示例：

- [SysMenuServiceImpl.java](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-admin/geshanzsq-nav-admin-system/src/main/java/com/geshanzsq/admin/system/menu/service/impl/SysMenuServiceImpl.java)

## 9. Mapper 与 SQL 规范

### 9.1 Mapper 约定

Mapper 接口统一：

- 使用 `@Mapper`
- 继承 `BaseMapperPlus<T>`

参考：

- [BaseMapperPlus.java](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/web/mapper/BaseMapperPlus.java)
- [SysMenuMapper.java](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-admin/geshanzsq-nav-admin-system/src/main/java/com/geshanzsq/admin/system/menu/mapper/SysMenuMapper.java)

### 9.2 SQL 放置规则

- 简单 CRUD 优先复用 MyBatis Plus 能力
- 复杂查询、关联查询写在 XML 中
- XML 文件与 Mapper 接口一一对应
- `mapper-locations` 统一加载 `classpath*:/mapper/**/*Mapper.xml`

### 9.3 查询写法建议

优先顺序：

1. 简单条件查询：`LambdaQueryWrapper`
2. 项目通用分页/条件构造：`BaseMapperPlus`
3. 复杂 SQL：Mapper XML

避免：

- 在 Service 中拼接原始 SQL
- Controller 中直接构造数据库查询条件

## 10. 分页规范

项目已有统一分页模型：

- 入参：`PageDTO`
- 出参：`PageVO<T>`

参考：

- [PageDTO.java](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/mybatis/page/dto/PageDTO.java)
- [PageVO.java](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-common/geshanzsq-nav-common-framework/src/main/java/com/geshanzsq/common/framework/mybatis/page/vo/PageVO.java)

规范要求：

- 分页查询 DTO 继承 `PageDTO`
- 前端分页字段保持 `pageNum`、`pageSize`
- 返回列表分页数据统一为 `list` + `total`

## 11. 注解与横切规范

常见统一注解应保持：

- `@RestController`
- `@RequestMapping`
- `@Api` / `@ApiOperation`
- `@PreAuthorize("@auth.hasUrl()")`
- `@Log(...)`
- `@Transactional(rollbackFor = Exception.class)`

规则：

- 有权限控制的后台接口，应补 `@PreAuthorize`
- 有新增、修改、删除语义的接口，应补 `@Log`
- 涉及多步写操作时，应补事务注解

## 12. 异常与校验规范

项目已有统一异常思路，建议遵循：

- 参数错误：抛 `ParamException`
- 业务错误：抛 `ServiceException`
- 参数基础校验：优先使用 `javax.validation`

不要：

- 手工返回各种不统一错误结构
- 在各处散落字符串状态码判断

## 13. 字段与数据库映射规范

- Java 字段使用小驼峰
- 数据库字段保持下划线命名
- 特殊字段通过 `@TableField` / `@TableId` 显式映射
- 逻辑删除遵守全局配置：
  - 删除值：`2`
  - 未删除值：`1`
  - 字段：`delFlag`

参考：

- [application.yml](/D:/Project/myself/geshanzsq-nav/geshanzsq-nav-admin/geshanzsq-nav-admin-application/src/main/resources/application.yml)

## 14. 依赖注入与代码风格

当前项目主要使用字段注入：

- `@Autowired private XxxService xxxService;`

这属于现有事实规范，新增代码为了保持一致，可以继续沿用。  
如果后续团队希望统一切换为构造器注入，应全项目一起调整，不建议单点混用过多风格。

其他风格约定：

- 类、字段、方法命名使用英文
- 注释允许中文，项目现有中文注释较多
- 常量、枚举单独抽取，不在业务代码里散落魔法值

## 15. 新增后端功能的推荐模板

新增一个业务模块时，建议至少包含：

1. `po`
2. `dto`
3. `vo`
4. `mapper`
5. `resources/mapper/*.xml`（如需要复杂 SQL）
6. `service`
7. `service/impl`
8. `controller`
9. `mapstruct`
10. `constant/enums`（如需要）

## 16. 当前项目中的“硬规则”与“推荐规则”区分

### 硬规则

- Java 版本为 `1.8`
- 编码为 `UTF-8`
- 使用 Maven 多模块
- 使用 `ResponseResult` 统一响应
- 使用 `MapStruct + Lombok`
- MyBatis Mapper XML 放在 `resources/mapper/**/*Mapper.xml`

### 推荐规则

- 不直接暴露 `PO`
- Controller 保持瘦身
- Service 负责业务和事务
- 复杂 SQL 下沉到 XML
- DTO/VO 命名沿用现有后缀体系
- 写操作补日志注解，事务操作补事务注解

## 17. 结论

本项目后端不是“依赖静态检查工具约束”的风格，而是“依赖统一分层和命名约定维持一致性”的风格。  
新增代码时，最重要的是保持以下四点一致：

- 分层一致
- 命名一致
- 转换方式一致
- 返回结构一致
