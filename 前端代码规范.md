# 前端代码规范

## 1. 适用范围

本文基于当前仓库前端模块的配置和代码实现归纳，适用于：

- `vue-geshanzsq-nav`

该文档既包含已有配置形成的硬规则，也包含从现有代码中提炼的项目约定。

## 2. 技术基线

- 框架：`Vue 3`
- UI：`Element Plus`
- 路由：`vue-router 4`
- 状态管理：`vuex 4`
- HTTP：`axios`
- 构建：`Vue CLI 5`
- 代码检查：`ESLint`
- 提交前检查：`lint-staged`
- 格式化：`Prettier`

配置来源：

- [package.json](/D:/Project/myself/geshanzsq-nav/vue-geshanzsq-nav/package.json)
- [vue.config.js](/D:/Project/myself/geshanzsq-nav/vue-geshanzsq-nav/vue.config.js)
- [.eslintrc.js](/D:/Project/myself/geshanzsq-nav/vue-geshanzsq-nav/.eslintrc.js)
- [.editorconfig](/D:/Project/myself/geshanzsq-nav/vue-geshanzsq-nav/.editorconfig)
- [.prettierrc](/D:/Project/myself/geshanzsq-nav/vue-geshanzsq-nav/.prettierrc)

## 3. 明确存在的硬规则

### 3.1 ESLint

当前 ESLint 配置：

- `extends: ['plugin:vue/vue3-essential', '@vue/standard']`
- parser：`@babel/eslint-parser`

额外规则：

- 生产环境 `no-console: warn`
- 生产环境 `no-debugger: warn`
- `space-before-function-paren: off`
- `vue/multi-word-component-names: off`

说明：

- 可以使用单词组件名，项目中确实存在 `index.vue`、`data.vue` 这类命名。
- 不要求函数名前必须有空格。

### 3.2 EditorConfig

针对 `js/jsx/ts/tsx/vue`：

- 使用空格缩进
- 缩进宽度：`2`
- 删除行尾空格
- 文件末尾保留换行

### 3.3 Prettier

当前 Prettier 规则：

- 不加分号：`semi: false`
- 使用单引号：`singleQuote: true`
- 不保留尾随逗号：`trailingComma: none`

### 3.4 提交前检查

`lint-staged` 当前规则：

- `*.{js,jsx,vue}` 提交前执行 `vue-cli-service lint`

说明：

- 目前 `ts`、`scss`、`md` 未配置提交前检查。

## 4. 目录与分层规则

前端目录组织较清晰，新增代码建议遵循现有结构：

- `src/api`：接口封装
- `src/views`：页面级组件
- `src/components`：通用组件
- `src/layout`：布局组件
- `src/router`：路由配置
- `src/store`：状态管理
- `src/utils`：通用工具
- `src/config`：前端配置
- `src/directive`：自定义指令
- `src/plugins`：插件注册

推荐职责划分：

- 页面交互写在 `views`
- 可复用能力下沉到 `components`
- 所有 HTTP 请求统一收敛到 `api`
- 不在页面里直接写裸 `axios`

## 5. API 层规范

项目 API 层风格非常统一，应继续保持：

- 每个业务域一个 API 文件或目录
- 从 `@/utils/request` 引入统一请求实例
- 每个接口导出独立函数
- 函数名使用业务语义，如 `page`、`getById`、`add`、`update`、`remove`

典型示例：

- [src/api/system/user.js](/D:/Project/myself/geshanzsq-nav/vue-geshanzsq-nav/src/api/system/user.js)

规范要求：

- GET 查询参数用 `params`
- POST/PUT 请求体用 `data`
- RESTful 路径参数优先使用模板字符串
- 不要在页面组件中直接写请求地址字符串

## 6. 请求与响应处理规范

项目通过统一的 `request` 实例处理：

- token 注入
- 表单编码转换
- 响应码判断
- 401 失效处理
- 错误消息弹出

参考：

- [src/utils/request.js](/D:/Project/myself/geshanzsq-nav/vue-geshanzsq-nav/src/utils/request.js)

规则：

- 所有请求必须走 `@/utils/request`
- 页面层只处理业务成功结果，不重复处理通用错误弹窗
- 下载、二进制流统一走已有响应分支

## 7. Vue 组件写法规范

### 7.1 主体风格

当前项目以 Vue 3 `script setup` 为主。

典型示例：

- [src/views/system/user/index.vue](/D:/Project/myself/geshanzsq-nav/vue-geshanzsq-nav/src/views/system/user/index.vue)

建议：

- 新页面优先使用 `script setup`
- 维持 `template` 在前、`script` 在后的结构
- 组件内部逻辑按“状态定义 -> 事件方法 -> 初始化调用”组织

### 7.2 命名规则

- 页面组件目录可用业务目录 + `index.vue`
- 子组件放在同级 `components` 目录
- 组件导入名使用 PascalCase
- 方法名使用 `handleXxx`、`getXxx`、`resetXxx` 等现有风格

现有常见命名：

- `handleQuery`
- `handleResetQuery`
- `handleDelete`
- `getPage`

### 7.3 响应式状态

项目当前主要使用：

- `ref`
- `getCurrentInstance`

规则：

- 表单对象、列表数据、loading、选中状态都使用响应式变量
- 对外部全局能力访问，沿用 `const { proxy } = getCurrentInstance()`

## 8. 模板层规范

项目页面模板遵循后台管理系统常见结构，新增页面建议保持一致：

- 页面根节点通常使用 `div.app-container`
- 查询区域使用 `el-form`
- 列表区域使用 `el-table`
- 分页统一使用全局 `Pagination`
- 新增/编辑弹窗拆成子组件

常见约定：

- 查询表单：`queryParams`
- 表格数据：`dataList`
- 总数：`totalCount`
- 加载状态：`loading`
- 单选状态：`single`
- 多选状态：`multiple`

## 9. 路由规范

路由分为：

- `constantRoutes`：公共路由
- `dynamicRoutes`：权限动态路由

参考：

- [src/router/index.js](/D:/Project/myself/geshanzsq-nav/vue-geshanzsq-nav/src/router/index.js)

规则：

- 页面路由信息集中维护，不在组件中临时拼装
- 需要权限控制的动态页面写入 `dynamicRoutes`
- 路由元信息统一写在 `meta`
- 后台管理页一般挂在 `Layout` 下

## 10. 状态管理规范

项目使用 Vuex，并自动加载 `store/modules` 下模块，统一加 `namespaced: true`。

参考：

- [src/store/index.js](/D:/Project/myself/geshanzsq-nav/vue-geshanzsq-nav/src/store/index.js)

规则：

- 新增 store 模块放在 `src/store/modules`
- 默认按命名空间使用
- 读取状态优先通过模块 getter
- 不要把页面临时状态塞进全局 store

## 11. 全局能力使用规范

项目已在 `main.js` 中注册多种全局能力：

- Element Plus
- 全局方法
- 全局组件
- 指令
- 插件

参考：

- [src/main.js](/D:/Project/myself/geshanzsq-nav/vue-geshanzsq-nav/src/main.js)

规则：

- 已全局注册的组件直接使用，不重复局部注册
- 通用方法优先复用 `globalProperties` 中已有能力
- 新的全局能力应在 `main.js` 或插件目录集中注册

## 12. 样式规范

根据当前配置和代码习惯，建议：

- 缩进 2 空格
- Vue 单文件组件内模板、脚本、样式风格保持统一
- 样式优先局部化，通用样式放公共样式目录

当前仓库未发现 Stylelint 规则，因此样式规范主要依赖团队自觉和组件内一致性。

## 13. 权限与指令规范

项目已有权限指令体系，例如：

- `v-permission`

规则：

- 按钮级权限控制优先用现有指令
- 不要在模板里散落复杂权限表达式
- 权限码命名应与后端保持一致

## 14. 表单与弹窗规范

现有页面中，新增/编辑/重置密码等操作普遍采用：

- 主页面负责列表和入口按钮
- 弹窗表单拆成子组件
- 父组件通过 `ref` 调用子组件 `show` 方法

建议保持：

- 列表页轻量
- 表单弹窗内聚
- 成功后通过事件或回调通知父页面刷新

## 15. 与后端接口对齐规范

前后端接口命名和数据结构已有稳定约定：

- 分页入参：`pageNum`、`pageSize`
- 分页返回：`total`、`list`
- 详情接口：`getById`
- 删除接口：`delete/{id}`

新增前端代码时应优先复用这些既有约定，避免同项目内出现两套命名。

## 16. 当前项目中的“硬规则”与“推荐规则”区分

### 硬规则

- 使用 ESLint：`plugin:vue/vue3-essential` + `@vue/standard`
- `js/jsx/ts/tsx/vue` 缩进 2 空格
- 使用单引号
- 不加分号
- 无尾随逗号
- `js/jsx/vue` 提交前执行 lint

### 推荐规则

- 请求统一走 `@/utils/request`
- 页面使用 `script setup`
- 页面逻辑命名使用 `handleXxx` / `getXxx`
- 列表页沿用 `queryParams`、`dataList`、`totalCount` 等命名
- 弹窗表单拆为子组件
- 新增路由按 `constantRoutes` / `dynamicRoutes` 现有结构维护

## 17. 结论

本项目前端已经具备明确的基础代码约束，核心是：

- ESLint
- EditorConfig
- Prettier
- 统一 API 封装
- 统一页面结构

新增代码时，优先保证以下一致性：

- 格式一致
- 请求封装一致
- 页面组织一致
- 路由与权限处理一致
